<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Cadastro de usuário</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    
    <form id="chat">
        <h1>Cadastro de usuário</h1></br>
        Nome: <input id="name" name="username" type="text" /><br />
        Email: <input id="mail" name="mail" type="text" /><br />
        Senha: <input id="password" name="password" type="password" /><br />
        Endereço: <input id="endereco" name="endereco" type="text" /><br />
        Celular: <input id="celular" name="celular" type="text" /><br />
        <button type="submit">Salvar</button>
    </form>

    <script>
        $('#chat').submit( function (event){
            event.preventDefault();
            var user = {
                name: $('#name').val(),
                mail: $('#mail').val(),
                password: $('#password').val(),
                endereco: $('#endereco').val(),
                celular: $('#celular').val()
            }
            
            $.ajax({
                url:'http://localhost:3000/user',
                type:'post',
                dataType:'json',
                contentType: 'application/json',
                success: function(data){
                    $('#name').val()
                    $('#mail').val()
                    $('#password').val()
                    $('#endereco').val()
                    $('#celular').val()
                    window.location = 'http://localhost:3000/';
                },
                data:JSON.stringify(user)
            });

        });  
    </script>
</body>

</html>